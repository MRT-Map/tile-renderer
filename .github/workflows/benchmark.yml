on:
  push:
    branches: [ "v5" ]
  workflow_dispatch:

permissions:
  contents: write
  
jobs:
  benchmark:
    runs-on: ubuntu-latest

    steps:
    - uses: actions/checkout@v4
    - uses: pypa/hatch@install
    - run: cargo install resvg usvg
    - run: hatch run python tests/test.py
